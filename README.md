# Upstream Sync

This orphan branch exists only to keep the fork synchronized.

- `main` is force-synchronized with `lidge-jun/opencodex:main`.
- `dev` contains personal changes and is never touched by the sync workflow.
- `upstream` contains only repository automation and must not be merged into
  either code branch.

The workflow runs every six hours and can also be started manually from the
Actions page.
